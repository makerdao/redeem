{
  "name": "redeem",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-animated-number": "^0.4.3",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "web3": "^0.20.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "presurge": "npm run build",
    "surge": "cd build && surge --domain https://redeem-mkr.surge.sh"
  }
}
